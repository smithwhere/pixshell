<Window x:Class="PixShell.HostEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="主机"
        Width="400" MinWidth="320" MinHeight="280" Height="520"
        SizeToContent="Manual"
        Tag="NoAutoResize"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="ToolWindow"
        Background="{DynamicResource BrushBg}"
        ShowInTaskbar="False"
        Foreground="{DynamicResource BrushText}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,4,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="48"/>
                <ColumnDefinition Width="*" MinWidth="120"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="类型" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <ComboBox  Grid.Row="0" Grid.Column="1" Name="TypeBox" Margin="8,4,0,4" SelectionChanged="OnTypeChanged">
                <ComboBoxItem Content="SSH"/>
                <ComboBoxItem Content="RDP（远程桌面）"/>
                <ComboBoxItem Content="Web（应用内页面/终端）"/>
            </ComboBox>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="名称" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <TextBox   Grid.Row="1" Grid.Column="1" Name="NameBox" Margin="8,4,0,4"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Name="HostLabel" Text="主机" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <TextBox   Grid.Row="2" Grid.Column="1" Name="HostBox" Margin="8,4,0,4"/>

            <!-- Web 专用：外部 noVNC / 面板 URL；留空 = 本地桥 WebSSH -->
            <TextBlock Grid.Row="3" Grid.Column="0" Name="WebUrlLabel" Text="URL" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}" Visibility="Collapsed"/>
            <TextBox   Grid.Row="3" Grid.Column="1" Name="WebUrlBox" Margin="8,4,0,4" Visibility="Collapsed"
                       ToolTip="https://…/vnc  （noVNC / 面板；留空=本地桥 WebSSH）"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="端口" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <TextBox   Grid.Row="4" Grid.Column="1" Name="PortBox" Text="22" Margin="8,4,0,4"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="用户名" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <TextBox   Grid.Row="5" Grid.Column="1" Name="UserBox" Margin="8,4,0,4"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="密码" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <PasswordBox Grid.Row="6" Grid.Column="1" Name="PassBox" Margin="8,4,0,4"/>

            <TextBlock Grid.Row="7" Grid.Column="0" Text="分组" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <TextBox   Grid.Row="7" Grid.Column="1" Name="GroupBox" Text="默认" Margin="8,4,0,4"/>

            <TextBlock Grid.Row="8" Grid.Column="0" Text="系统" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <ComboBox  Grid.Row="8" Grid.Column="1" Name="OsBox" Margin="8,4,0,4" IsEditable="False">
                <ComboBoxItem Content=""/>
                <ComboBoxItem Content="linux"/>
                <ComboBoxItem Content="ubuntu"/>
                <ComboBoxItem Content="debian"/>
                <ComboBoxItem Content="centos"/>
                <ComboBoxItem Content="alpine"/>
                <ComboBoxItem Content="openwrt"/>
                <ComboBoxItem Content="windows"/>
                <ComboBoxItem Content="macos"/>
                <ComboBoxItem Content="web"/>
            </ComboBox>

            <TextBlock Grid.Row="9" Grid.Column="0" Text="私钥" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <Grid Grid.Row="9" Grid.Column="1" Margin="8,4,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="80"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Name="KeyPathBox"
                         ToolTip="留空则使用密码登录" Margin="0,0,6,0"/>
                <Button  Grid.Column="1" Content="选择…" MinWidth="56" Padding="8,3" Click="OnChooseKeyFile" Style="{StaticResource PillButton}"/>
            </Grid>

            <TextBlock Grid.Row="10" Grid.Column="0" Text="私钥口令" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"
                       ToolTip="私钥口令（Key Passphrase）是保护私钥文件本身的密码，与服务器登录密码不同。Debian/OpenSSH 生成的 id_ed25519 若设置了口令，请在此填写。口令会使用 Windows DPAPI 安全加密保存。"/>
            <PasswordBox Grid.Row="10" Grid.Column="1" Name="KeyPassBox" Margin="8,4,0,4"
                         ToolTip="私钥文件的口令（Key Passphrase），非服务器登录密码。编辑时留空表示保持原口令不变；如需清除已保存的口令，请删除私钥路径后重新保存。"/>

            <TextBlock Grid.Row="11" Grid.Column="0" Text="代理" VerticalAlignment="Center" Margin="0,4"
                       Foreground="{DynamicResource BrushMuted}"/>
            <ComboBox  Grid.Row="11" Grid.Column="1" Name="ProxyBox" Margin="8,4,0,4"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="保存" Tag="Primary" Style="{StaticResource PillButton}" MinWidth="72" Padding="14,4" Margin="0,0,8,0" IsDefault="True" Click="OnSave"/>
            <Button Content="取消" Style="{StaticResource PillButton}" MinWidth="72" Padding="14,4" IsCancel="True" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
